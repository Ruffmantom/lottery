
// need to make a event listener to know when someone clicks the button
var click = document.addEventListener('click', (e) => console.log(e));

// need to make a function that when the button does get clicked it sends an output to the user

let output = []; 

var yourNumber = () => {
    for (i = 0, i <= 10, i++) {
        output =[i];
    }
}
console.log(yourNumber);
